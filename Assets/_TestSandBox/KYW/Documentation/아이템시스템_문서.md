# 🗡️ 아이템 시스템 문서

## 📋 개요
멀티플레이어 게임을 위한 근접 무기 시스템입니다. Photon Fusion2를 사용하여 네트워크 동기화를 구현했습니다.

## 🏗️ 프리팹 구조

### 곡괭이 프리팹 구조
```
곡괭이 (메인 오브젝트)
├── 컴포넌트:
│   ├── Transform
│   ├── Box Collider 2D (2개) - 기본콜라이더
│   ├── Composite Collider 2D
│   ├── Rigidbody 2D
│   ├── Pickaxe (Script)
│   ├── Network Rigidbody 2D (Script)
│   └── Network Object
├── 곡괭이이미지 (시각적 표현)
├── 어택콜라이더 (공격용 콜라이더)
│   └── 스크립트: AttackCollisionHandler
└── 스피드콜라이더 (속도 기반 공격용)
    └── 스크립트: SpeedCollisionHandler
```

### 구조 설명
- **기본콜라이더**: 부모 오브젝트에 직접 배치 (Box Collider 2D 2개 + Composite Collider 2D)
- **공격콜라이더**: 별도 자식 오브젝트로 분리
- **속도콜라이더**: 별도 자식 오브젝트로 분리

## 🔧 핵심 스크립트

### 1. IItemInteraction (인터페이스)
```csharp
public interface IItemInteraction : IKnockbackable, IHoldable, IUsableItem
{
    // 넉백
    new void ApplyKnockback(Vector2 force, float duration = 0f);

    // 들기
    bool IsHeld { get; }  // 현재 들려있는지 여부
    new void OnPickedUp();
    new void OnReleased();

    // 사용 (클릭 입력 기반)
    new void OnUsePress(Vector2 mouseWorldPosition, Vector2 playerPosition);
    new void OnUseHold(Vector2 mouseWorldPosition, Vector2 playerPosition);
    new void OnUseRelease(Vector2 mouseWorldPosition, Vector2 playerPosition);
}
```

**주요 메서드:**
- **`IsHeld`**: 아이템이 현재 들려있는지 여부 (IsHoldable 대체)
- **`OnPickedUp()`**: 아이템을 집었을 때 호출
- **`OnReleased()`**: 아이템을 놓았을 때 호출
- **`ApplyKnockback()`**: 넉백 힘 적용
- **`OnUsePress()`**: 마우스 클릭 시 호출 (곡괭이 휘두르기 등)
- **`OnUseHold()`**: 마우스 홀드 시 호출 (연속 공격 등)
- **`OnUseRelease()`**: 마우스 릴리즈 시 호출

### 2. Pickaxe.cs (곡괭이 스크립트)
```csharp
public class Pickaxe : NetworkBehaviour, IItemInteraction
{
    [Header("Pickaxe Settings")]
    [SerializeField] private float swingAngle = 90f;
    [SerializeField] private float swingDuration = 0.18f;
    
    [Header("Attack Collision Handler")]
    [SerializeField] private AttackCollisionHandler attackCollisionHandler;
    
    [Networked] private TickTimer PickaxeTimer { get; set; }
    [Networked] private NetworkBool IsHeld { get; set; }
    
    private bool isPickaxeActive => PickaxeTimer.IsRunning;
    bool IItemInteraction.IsHeld => IsHeld;
    
    // 공격 로직, 타이머 관리, 애니메이션 등
}
```

**주요 기능:**
- 곡괭이 휘두르기 애니메이션
- 타이머 기반 공격 지속시간 관리
- 들기/놓기 상태 관리
- AttackCollisionHandler와 연동

### 3. AttackCollisionHandler.cs
**역할**: 아이템 사용 시 충돌 처리

```csharp
public class AttackCollisionHandler : NetworkBehaviour
{
    [Header("Attack Settings")]
    [SerializeField] private int attackDamage = 1;
    [SerializeField] private float knockbackForce = 5f;
    [SerializeField] private float knockbackDuration = 0.5f;
    
    [Header("Collider Reference")]
    [SerializeField] public Collider2D AttackCollider;
    
    private IItemInteraction weaponItem;
}
```

**주요 기능:**
- 공격 콜라이더 활성화 시 충돌 감지
- 플레이어/NPC: 데미지 + 넉백 + 무적시간 적용
- 아이템: 넉백만 적용
- 같은 아이템 내부 충돌 방지 (`IsSameItem()` 메서드)
- 들린 상태에서만 작동 (`IsHeld = true`)

### 4. SpeedCollisionHandler.cs
**역할**: 아이템이 던져지거나 빠르게 움직일 때 충돌 처리

```csharp
public class SpeedCollisionHandler : NetworkBehaviour
{
    [Header("Speed Attack Settings")]
    [SerializeField] private float attackSpeedThreshold = 0.3f;  // 공격 콜라이더 활성화 속도 임계값
    [SerializeField] private float normalSpeedThreshold = 0.05f;  // 일반 콜라이더로 되돌릴 속도 임계값
    
    [Header("Collision Settings")]
    [SerializeField] private int speedAttackDamage = 1;
    [SerializeField] private float speedAttackKnockbackForce = 5f;
    [SerializeField] private float speedAttackKnockbackDuration = 0.5f;
    
    [Header("Collider References")]
    [SerializeField] private Collider2D speedAttackCollider;    // SpeedAttack 레이어 콜라이더
    
    [Networked] private NetworkBool IsInSpeedAttackMode { get; set; }
}
```

**주요 기능:**
- 속도 임계값에 따른 콜라이더 자동 활성화/비활성화
- 들리지 않은 상태에서만 작동 (`IsHeld = false`)
- 충돌 시 데미지 + 넉백 적용
- 같은 아이템 내부 충돌 방지 (`IsSameItem()` 메서드)
- 부모 오브젝트의 Rigidbody2D 속도 체크

## ⚡ 작동 방식

### 1. 곡괭이 사용 (AttackCollisionHandler)
```
1. 플레이어가 곡괭이 집기 → IsHeld = true
2. 마우스 클릭 → PickaxeTimer 시작
3. 어택콜라이더 활성화 → AttackCollisionHandler 작동
4. 충돌 감지 시:
   - 플레이어/NPC: 데미지 + 넉백 + 무적시간
   - 아이템: 넉백만
   - 같은 아이템 내부: 무시
5. 타이머 종료 → 어택콜라이더 비활성화
```

### 2. 곡괭이 던지기 (SpeedCollisionHandler)
```
1. 플레이어가 곡괭이 놓기 → IsHeld = false
2. 곡괭이가 빠르게 움직임 → 속도 ≥ 0.3f
3. 스피드콜라이더 활성화 → SpeedCollisionHandler 작동
4. 충돌 시 데미지 + 넉백 (같은 아이템 내부 충돌 방지)
5. 속도 감소 → 속도 ≤ 0.05f
6. 스피드콜라이더 비활성화
```

### 3. 상태 관리
```
IsHeld = true  → 아이템이 들린 상태
IsHeld = false → 아이템이 바닥에 있는 상태

기존 IsHoldable은 IsHeld로 통합
- 들린 상태: IsHeld = true (들 수 없음)
- 바닥 상태: IsHeld = false (들 수 있음)
```

## 🔗 인스펙터 연결

### 필수 연결 작업:
1. **AttackCollisionHandler** → `Attack Collider` 필드에 어택콜라이더의 `Collider2D` 연결
2. **SpeedCollisionHandler** → `Speed Attack Collider` 필드에 스피드콜라이더의 `Collider2D` 연결
3. **Pickaxe** → `Attack Collision Handler` 필드에 어택콜라이더의 `AttackCollisionHandler` 스크립트 연결

### 연결 방법:
- 인스펙터에서 해당 필드에 드래그 앤 드롭으로 연결
- 프리팹 구조가 일정하므로 한 번 설정하면 재사용 가능

## 🛡️ 안전장치

### 1. 자기 자신 맞기 방지
```csharp
private bool IsSameItem(GameObject other)
{
    // 자기 자신
    if (other == gameObject) return true;
    
    // 부모가 같은지 확인 (같은 아이템의 다른 콜라이더)
    if (weaponItem != null)
    {
        var otherItem = other.GetComponentInParent<IItemInteraction>();
        if (otherItem != null && otherItem == weaponItem) return true;
    }
    
    // 자식인지 확인
    if (other.transform.IsChildOf(transform)) return true;
    
    // 부모인지 확인
    if (transform.IsChildOf(other.transform)) return true;
    
    return false;
}
```

### 2. 무적 상태 보호
```csharp
// PlayerInteractionBase에서 무적 상태일 때 보호
if (stunInvincible?.IsInvincible == true) return;
```

### 3. 권한 체크
```csharp
if (!HasStateAuthority) return;  // StateAuthority만 처리
```

### 4. 넉백 방향 계산
```csharp
// 콜라이더의 실제 중심점을 기준으로 정확한 방향 계산
Vector2 knockbackDirection = ((Vector2)target.transform.position - (Vector2)AttackCollider.bounds.center).normalized;
```

## 🎮 사용 예시

### 곡괭이 휘두르기:
1. 플레이어가 곡괭이 집기
2. 마우스 클릭으로 공격 시작
3. 곡괭이가 90도 회전하며 휘두르기
4. 어택콜라이더가 활성화되어 충돌 감지
5. 0.18초 후 공격 종료

### 곡괭이 던지기:
1. 플레이어가 곡괭이 놓기
2. 곡괭이가 물리 법칙에 따라 움직임
3. 속도가 0.3f 이상이면 스피드콜라이더 활성화
4. 충돌 시 데미지 + 넉백 적용 (같은 아이템 내부 충돌 방지)
5. 속도가 0.05f 이하로 떨어지면 스피드콜라이더 비활성화

## 🔄 확장 방법

### 새로운 아이템 추가:
1. 곡괭이 프리팹을 복사
2. 아이템 스크립트 생성 (IItemInteraction 구현)
3. AttackCollisionHandler, SpeedCollisionHandler 연결
4. 인스펙터에서 콜라이더 연결
5. 아이템별 고유 로직 구현

### 새로운 기능 추가:
1. IItemInteraction 인터페이스에 메서드 추가
2. 각 핸들러에서 해당 로직 구현
3. 아이템 스크립트에서 호출

## 📝 주의사항

1. **네트워크 동기화**: 모든 상태 변경은 `[Networked]` 프로퍼티 사용
2. **권한 체크**: `HasStateAuthority` 확인 필수
3. **타이머 사용**: `TickTimer`로 시간 기반 로직 구현
4. **콜라이더 연결**: 인스펙터에서 수동 연결 필요
5. **충돌 레이어**: 적절한 레이어 설정으로 충돌 제어
6. **부모 해제 지연**: 아이템 던질 때 부모 해제가 0.05초 지연되어 자기 자신과의 충돌 방지

---

**이 구조로 다양한 근접 무기를 쉽게 추가할 수 있습니다!** 🎯

# 패시브 아이템 시스템 문서

## 개요
패시브 아이템은 사용 아이템과 동일한 방식으로 구현되며, 사용하면 플레이어에게 지속적인 효과를 부여하는 아이템입니다. 들고 있는 상태에서 클릭하면 사용되어 인벤토리에 효과가 저장되고 아이템은 사라집니다.

## 구현 방식

### 1. 기본 구조
- 사용 아이템과 동일한 `IItemInteraction` 인터페이스 구현
- `OnUsePress`에서 효과 적용 후 아이템 제거
- 플레이어 컨트롤러에서 인벤토리 상태에 따라 효과 적용
- 기존 아이템 시스템과 완전히 호환

### 2. 핵심 아이디어
- **일관성**: 기존 사용 아이템과 동일한 구조
- **단순성**: 복잡한 패시브 시스템 없이 기존 시스템 활용
- **확장성**: 새로운 패시브 아이템 추가가 매우 간단

## 패시브 아이템 종류

### 1. 날개 (Wings)
**기능**: 2단 점프 가능

#### 구현 세부사항
- **효과**: 기본 점프 후 추가 점프 1회 가능
- **조건**: 공중에서 점프 키 입력 시
- **제한**: 지면 착지 시 점프 카운트 리셋

#### 패시브 아이템 구현
```csharp
public class Wings : NetworkBehaviour, IItemInteraction
{
    [Networked] private NetworkBool IsHeld { get; set; }
    bool IItemInteraction.IsHeld => IsHeld;

    public void OnUsePress(Vector2 mouseWorldPosition, Vector2 playerPosition)
    {
        var playerController = FindFirstObjectByType<PlayerController>();
        if (playerController != null)
        {
            playerController.hasWings = true; // 직접 접근
        }
        RemoveFromHand(); // 손에서 제거 및 Despawn
    }
    
    // ... OnUseHold, OnUseRelease, OnPickedUp, OnReleased, ApplyKnockback ...
    
    private void RemoveFromHand()
    {
        // 기존 던지기 시스템 활용 (힘 없이)
        var playerThrower = FindFirstObjectByType<PlayerObjectThrower>();
        if (playerThrower != null)
        {
            playerThrower.ReleaseObject(gameObject, false); // applyForce = false
        }
        // 물리 활성화는 상관없음 (어차피 바로 Despawn)
        Runner.Despawn(Object);
    }
}
```

#### 플레이어 컨트롤러 수정
```csharp
public class PlayerController : NetworkBehaviour
{
    [Networked] public NetworkBool hasWings { get; set; }
    [Networked] private int jumpCount { get; set; } = 0;
    
    private void HandleJump()
    {
        int maxJumps = hasWings ? 2 : 1;
        if (jumpCount < maxJumps && jumpInput)
        {
            jumpCount++;
        }
    }
    private void OnGroundHit() { jumpCount = 0; }
}
```

### 2. 제트팩 (Jetpack)
**기능**: 연료를 소모하여 지속 점프

#### 구현 세부사항
- **효과**: 점프 후 점프 키를 누르고 있으면 연료 소모하며 위로 힘
- **연료**: 100%에서 시작, 사용 시 감소
- **조건**: 공중에서 점프 키 홀드

#### 패시브 아이템 구현
```csharp
public class Jetpack : NetworkBehaviour, IItemInteraction
{
    [Networked] private NetworkBool IsHeld { get; set; }
    bool IItemInteraction.IsHeld => IsHeld;

    public void OnUsePress(Vector2 mouseWorldPosition, Vector2 playerPosition)
    {
        var playerController = FindFirstObjectByType<PlayerController>();
        if (playerController != null)
        {
            playerController.hasJetpack = true;
        }
        RemoveFromHand();
    }
    
    private void RemoveFromHand()
    {
        var playerThrower = FindFirstObjectByType<PlayerObjectThrower>();
        if (playerThrower != null)
        {
            playerThrower.ReleaseObject(gameObject, false);
        }
        Runner.Despawn(Object);
    }
}
```

#### 플레이어 컨트롤러 수정
```csharp
public class PlayerController : NetworkBehaviour
{
    [Networked] public NetworkBool hasJetpack { get; set; }
    [Networked] public float jetpackFuel { get; set; } = 100f;
    [Networked] private NetworkBool isUsingJetpack { get; set; }
    
    private void HandleJetpack()
    {
        if (hasJetpack && jetpackFuel > 0 && jumpInput && !isGrounded)
        {
            isUsingJetpack = true;
            jetpackFuel -= Time.deltaTime * 20f; // 초당 20% 소모
            rb.AddForce(Vector2.up * 15f, ForceMode2D.Force);
        }
        else
        {
            isUsingJetpack = false;
        }
    }
}
```

### 3. 스피드 신발 (SpeedShoes)
**기능**: 이동 속도 증가

#### 구현 세부사항
- **효과**: 기본 이동 속도의 1.5배
- **지속**: 영구적 (게임 종료까지)

#### 패시브 아이템 구현
```csharp
public class SpeedShoes : NetworkBehaviour, IItemInteraction
{
    [Networked] private NetworkBool IsHeld { get; set; }
    bool IItemInteraction.IsHeld => IsHeld;

    public void OnUsePress(Vector2 mouseWorldPosition, Vector2 playerPosition)
    {
        var playerController = FindFirstObjectByType<PlayerController>();
        if (playerController != null)
        {
            playerController.hasSpeedShoes = true;
        }
        RemoveFromHand();
    }
    
    private void RemoveFromHand()
    {
        var playerThrower = FindFirstObjectByType<PlayerObjectThrower>();
        if (playerThrower != null)
        {
            playerThrower.ReleaseObject(gameObject, false);
        }
        Runner.Despawn(Object);
    }
}
```

#### 플레이어 컨트롤러 수정
```csharp
public class PlayerController : NetworkBehaviour
{
    [Networked] public NetworkBool hasSpeedShoes { get; set; }
    
    private void HandleMovement()
    {
        float speed = hasSpeedShoes ? moveSpeed * 1.5f : moveSpeed;
        // ... 기존 이동 로직에 speed 적용
    }
}
```

### 4. 미러볼 (MirrorBall)
**기능**: 스프라이트 머티리얼 반짝임 효과

#### 구현 세부사항
- **효과**: 스프라이트 머티리얼을 주기적으로 변경하여 반짝임
- **주기**: 0.1초마다 머티리얼 변경
- **머티리얼**: 미리 정의된 반짝임 머티리얼 배열 사용

#### 패시브 아이템 구현
```csharp
public class MirrorBall : NetworkBehaviour, IItemInteraction
{
    [Networked] private NetworkBool IsHeld { get; set; }
    bool IItemInteraction.IsHeld => IsHeld;

    public void OnUsePress(Vector2 mouseWorldPosition, Vector2 playerPosition)
    {
        var playerController = FindFirstObjectByType<PlayerController>();
        if (playerController != null)
        {
            playerController.hasMirrorBall = true;
        }
        RemoveFromHand();
    }
    
    private void RemoveFromHand()
    {
        var playerThrower = FindFirstObjectByType<PlayerObjectThrower>();
        if (playerThrower != null)
        {
            playerThrower.ReleaseObject(gameObject, false);
        }
        Runner.Despawn(Object);
    }
}
```

#### 플레이어 컨트롤러 수정
```csharp
public class PlayerController : NetworkBehaviour
{
    [Networked] public NetworkBool hasMirrorBall { get; set; }
    [Networked] public TickTimer sparkleTimer { get; set; }
    [Networked] private int currentSparkleIndex { get; set; }
    
    [Header("MirrorBall Settings")]
    [SerializeField] private Material[] sparkleMaterials;
    private SpriteRenderer spriteRenderer;
    
    private void HandleMirrorBall()
    {
        if (hasMirrorBall && sparkleTimer.Expired(Runner))
        {
            sparkleTimer = TickTimer.CreateFromSeconds(Runner, 0.1f);
            currentSparkleIndex = (currentSparkleIndex + 1) % sparkleMaterials.Length;
            spriteRenderer.material = sparkleMaterials[currentSparkleIndex];
        }
    }
}
```

## 통합 구현 구조

### 1. 플레이어 컨트롤러 통합 관리
```csharp
public class PlayerController : NetworkBehaviour
{
    // 패시브 아이템 보유 상태
    [Networked] public NetworkBool hasWings { get; set; }
    [Networked] public NetworkBool hasJetpack { get; set; }
    [Networked] public NetworkBool hasSpeedShoes { get; set; }
    [Networked] public NetworkBool hasMirrorBall { get; set; }
    
    // 제트팩 전용 변수
    [Networked] public float jetpackFuel { get; set; } = 100f;
    [Networked] private NetworkBool isUsingJetpack { get; set; }
    
    // 미러볼 전용 변수
    [Networked] public TickTimer sparkleTimer { get; set; }
    [Networked] private int currentSparkleIndex { get; set; }
    
    // 점프 시스템
    [Networked] private int jumpCount { get; set; } = 0;
    
    public override void FixedUpdateNetwork()
    {
        // 기존 이동/점프 로직...
        if (hasJetpack) HandleJetpack();
        if (hasMirrorBall) HandleMirrorBall();
    }
    // ... HandleJump(), HandleMovement(), HandleJetpack(), HandleMirrorBall(), OnGroundHit() ...
}
```

### 2. 사용 방법
1. **아이템 획득**: 기존 아이템 픽업 시스템 사용
2. **아이템 사용**: 들고 있는 상태에서 클릭
3. **효과 적용**: 직접 bool 변수 설정으로 효과 부여
4. **아이템 제거**: 기존 던지기 시스템 활용 후 `Runner.Despawn()`

## 네트워크 동기화 고려사항
- 모든 패시브 아이템 상태는 `[Networked]` bool 변수로 간단하게 관리
- 불필요한 Update 호출 제거: 아이템이 있을 때만 해당 로직 실행
- 네트워크 부담 최소화: 필요한 상태만 동기화
- 미러볼의 머티리얼 변경은 모든 클라이언트에서 동기화
- 제트팩 사용 시 애니메이션 효과 추가 가능

## 확장성
### 1. 새로운 패시브 아이템 추가
1. **아이템 스크립트 생성**: `IItemInteraction` 구현
2. **플레이어 컨트롤러 수정**: bool 변수와 처리 로직 추가
3. **직접 접근**: 아이템에서 직접 bool 변수 설정
4. **기존 시스템 활용**: `PlayerObjectThrower.ReleaseObject()` 사용